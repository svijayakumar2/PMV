# End-to-end PMV study config on math.
# Use this to diagnose whether collapse is Zebra-specific or loop-wide.

model:
  prover_model: "Qwen/Qwen2.5-3B-Instruct"
  verifier_model: "Qwen/Qwen2.5-3B-Instruct"
  num_verifiers: 3
  use_quantization: true
  prover_lora_r: 8
  prover_lora_alpha: 16
  verifier_lora_r: 8
  verifier_lora_alpha: 16
  aggregator_hidden_dim: 64

dataset:
  type: math
  num_samples: 50000

training:
  oversight_rule: "supervised"
  seed: 0
  mu_0: 0.5

  rounds: 3
  bootstrap_episodes: 40
  bootstrap_oracle_episodes: 40
  collect_episodes: 50
  helpful_warmup_steps: 40

  verifier_lr: 1.0e-4
  verifier_epochs: 3
  verifier_batch_size: 8
  verifier_max_length: 512
  auto_adjust_verifier_max_length: true
  verifier_prompt_margin: 256
  phase1_micro_step: false
  phase1_balance_labels: true
  phase1_stratified_batches: true
  verifier_dropout: 0.0
  softmin_beta: 5.0

  helpful_do_sample: false
  helpful_temperature: 0.2
  helpful_max_new_tokens: 256
  sneaky_do_sample: true
  sneaky_temperature: 0.9
  sneaky_max_new_tokens: 256

  helpful_warmup_max_length: 1024
  ppo_max_length: 1024

  oversight_decision_threshold: 0.5
  verifier_decision_threshold: 0.5
  sneaky_fool_threshold: 0.5
